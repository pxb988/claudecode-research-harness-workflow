# 合并报告

> 每个合并操作填写一个报告条目。由 `/research-harness-clean` 生成。
> 在本报告填写完毕且验证部分通过之前，合并都不算完成。
> 归档于 `4.reports/merge_report.md`。每个合并追加一个条目。

---

## 合并条目

### 合并元信息

- **合并步骤：** 1（清洗流程中每个合并递增）
- **日期：** YYYY-MM-DD
- **脚本：** `0.dofiles/clean.R`（行：unknown）
- **日志：** `0.dofiles/logs/clean.log`

---

### 合并规格

| 字段 | 值 |
|---|---|
| 合并类型 | `1:1` / `m:1` / `1:m` / `m:m` |
| 左表（主表）文件 | `1.rawdata/` 或 `2.workdata/` |
| 右表（从表）文件 | `1.rawdata/` 或 `2.workdata/` |
| 合并键 | （列出所有连接变量） |
| 输出文件 | `2.workdata/` 或 `3.outdata/data/` |

---

### 行数

| 数据集 | 合并前行数 |
|---|---|
| 左表（主表） | 0 |
| 右表（从表） | 0 |
| **合并后** | **0** |

| 结果 | 计数 |
|---|---|
| 已匹配（双方都有） | 0 |
| 未匹配 — 仅左表 | 0 |
| 未匹配 — 仅右表 | 0 |

**匹配率：** 0 / 0 = 0.0%

---

### 重复键诊断

| 检查项 | 结果 |
|---|---|
| 左表键唯一 | YES / NO |
| 右表键唯一 | YES / NO |
| 左表重复键 | （如有则给出计数和示例） |
| 右表重复键 | （如有则给出计数和示例） |
| 对重复采取的处理 | none / dropped / flagged |

---

### 变量冲突

列出在两个文件中都存在、且同一键下取值不同的变量。

| 变量 | 解决方式 | 备注 |
|---|---|---|
| | keep-left / keep-right / create both with suffix / flagged | |

若存在冲突且未指定解决方式：停下——不要默默选择其一。在此记录并请求澄清。

---

### 未匹配观测处理

| 未匹配集合 | 计数 | 处理 | 原因 |
|---|---|---|---|
| 仅左表 | 0 | keep / drop / flag | |
| 仅右表 | 0 | keep / drop / flag | |

若丢弃未匹配观测：记录计数和原因。

---

### 未决问题

| 问题 | 严重度 | 状态 |
|---|---|---|
| | `blocking` / `non-blocking` | open / resolved |

---

### 验证

- [ ] 脚本运行无错误
- [ ] 日志显示合并诊断信息
- [ ] 上述行数与日志输出一致
- [ ] 重复键检查通过（或已记录例外）
- [ ] 变量冲突已解决（或已标记并等待澄清）
- [ ] 未匹配观测已按计划处理

**验证状态：** PASS / FAIL / PARTIAL

---

<!-- 流程中每增加一个合并，复制粘贴上面的整块 -->
